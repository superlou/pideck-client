<div class="header">
  <div class="now-playing">
    {{#if player.isLoaded}}
      {{player.source}}
    {{else}}
      (no file loaded)
    {{/if}}
  </div>

  <div class="playback-controls">
    <button {{action "togglePause"}} disabled={{player.isNotLoaded}}>
      {{#if player.isPlaying}}
        {{fa-icon "pause"}}
      {{else}}
        {{fa-icon "play"}}
      {{/if}}
    </button>
    <button {{action "stop"}} disabled={{player.isNotLoaded}}>
      {{fa-icon "stop"}}
    </button>

    <ul class="button-group">
      <li><button {{action "volumeMute"}} disabled={{player.isNotLoaded}}>{{fa-icon "volume-off"}}</button></li>
      <li><button {{action "volumeDown"}} disabled={{player.isNotLoaded}}>{{fa-icon "volume-down"}}</button></li>
      <li><button {{action "volumeUp"}} disabled={{player.isNotLoaded}}>{{fa-icon "volume-up"}}</button></li>
    </ul>
  </div>
</div>

{{playback-bar
  duration=player.duration
  position=player.position
  seek=(action "seek")
}}
